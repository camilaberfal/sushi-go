-- Seed baseline for local development.
-- This project relies on authenticated users from auth.users,
-- so game data seeding is executed by app flows after sign-in.

select 'sushi-go seed ready' as message;
